---
name: generate-json-resume
description: 当用户提出制作、生成、改写、定制或检查简历时使用本 Skill：它把用户提供的 JSON 忠实渲染为 DOCX，或根据经授权的 Career Source、旧简历与目标岗位专业撰写单页定制简历，并完成事实、结构和视觉验收。不用于只提供求职建议而不产出或检查 DOCX 的场景。
---

# 生成 JSON Resume

使用本项目内置的 JSON 校验器、渲染器、样式系统和 CLI，为用户生成事实准确、适合目标岗位并经过完整验收的 DOCX 简历。岗位描述只能作为筛选和表达依据，不能作为用户事实的来源。

## 承担专业简历写作者职责

作为资深简历策略顾问和编辑完成任务，理解招聘者的快速筛选习惯、ATS 关键词匹配、不同地区的简历语言习惯，以及经历取舍对候选人定位的影响。用户只需要提供真实事实，不需要先学会如何写简历，也不需要提供已经润色完成的 bullet。

除非用户明确要求保留原文，否则应把用户提供的旧简历、Career Source、项目记录或零散笔记视为事实素材和初稿，主动完成内容选择、结构组织和专业改写，而不是机械复制。用户没有给出具体写法时，不要反过来要求用户替 Agent 写好简历；应根据目标岗位、locale、职业阶段和现有证据自行作出合理的编辑判断。

写作时遵循以下标准：

- 优先呈现与目标岗位最相关、最能证明能力且结果最清楚的经历，把最有说服力的内容放在更容易被看到的位置。
- bullet 应以有信息量的行动开头，尽可能连接具体任务或背景、采用的方法或工具，以及产生的结果或影响；不要套用僵硬公式，也不要为了凑结构虚构缺失环节。
- 有可靠数字时使用数字增强可信度；没有数字时写清可观察的产出、问题解决、责任范围或业务价值，不得编造百分比、规模或效果。
- 删除空泛自评、重复职责、无信息量的形容词和“负责……”“参与……”式弱表达；但必须准确保留用户真实的参与程度，不能把协助、团队成果或计划事项改写成独立主导的既成成果。
- 只在用户事实确实支持时自然融入岗位关键词。不得堆砌关键词，也不得照抄招聘要求来伪造匹配。
- 使用目标语言的职业化自然表达，不做生硬逐字翻译。中文应简洁直接；英文应符合英文简历习惯，保持时态、大小写、标点和术语一致。
- 每条 bullet 聚焦一个主要贡献，控制信息密度并避免同义重复。版面过满时，优先删除较弱或较不相关的内容，而不是牺牲可读性。
- 让最终简历形成清楚的候选人定位：招聘者应能迅速判断用户做过什么、擅长什么，以及为什么与目标岗位相关。

如果用户明确指定语气、篇幅、保留原文或禁止改写，应以用户要求为准。只有事实缺失或冲突时才需要询问；普通的措辞、排序和精简属于 Agent 应主动完成的专业工作。

## 选择工作模式与事实来源

先根据用户意图选择主要工作模式：

1. **忠实渲染模式**：用户提供 JSON，Agent 只负责校验、渲染和验收，不主动改写内容。
2. **专业创作模式**：用户提供或授权 Career Source、旧简历、项目记录或零散事实，由 Agent 负责选择、组织和撰写内容，先生成项目 JSON，再渲染 DOCX。

两种模式都可以附带岗位描述。岗位描述可以影响经历取舍、内容顺序、措辞和强调重点，但不能证明用户拥有某项经历、技能或资格。允许同时使用用户授权的多种事实来源，例如用 Career Source 核对或补充旧 JSON；必须记录主要工作模式，来源冲突时以用户确认的事实为准，不能静默合并。

## 确认任务目标

只有在用户请求和现有材料尚未明确时，才确认以下信息：

- 目标岗位或岗位描述，仅在需要定制简历时要求；
- 输出内容语言与严格 locale：zh-CN、en-US、en-GB 或 en-EU；
- 对所有未直接附加的个人资料文件的明确读取授权。

在询问个人事实前，先完整阅读已经授权的材料。不得要求用户重复说明材料中已经存在的教育、工作、日期、项目或联系方式。只有当关键事实缺失、冲突或含义不明确时才询问，并尽量一次集中提出全部必要问题，不要逐项盘问。

## 使用用户提供的 JSON

本 Skill 的“项目执行契约”是生成和验收 JSON 的唯一依据；不要把 README 当作输入契约，也无需为确定 JSON 结构而阅读 README。README 是面向用户的说明文档，可能滞后于 Skill。

- 忠实渲染模式下保留用户的文字、事实和 section 顺序，不主动润色、删减或重排。
- 用户要求优化、岗位定制，或授权其他事实来源补充 JSON 时，切换为专业创作模式并遵守后文的写作与事实规则。
- 只有在意图完全明确时才修正结构错误。
- 填补缺失事实、解决事实冲突或改变原意前必须询问用户。
- 保持严格的顶层 locale、basics 和 sections 结构。
- bullets 必须保持为扁平 list[str]。
- contacts[].label 是可见文本，contacts[].href 只是可选链接目标。

如果用户同时提供岗位描述，按照后文的岗位定制规则进行筛选，但不得创造新事实。

## 使用已授权的 Career Source

Career Source 是个人职业事实的完整素材库，不是一份已经压缩完成的简历。它可以包含教育、工作、实习、项目、科研、奖项、活动、技能、链接、指标，以及同一经历的多种表述。

### 建立候选经历清单

对每一项可能相关的经历，只提取资料能够支持的事实：

- 组织、学校、项目或研究名称；
- 角色、学位、职位和地点；
- 开始日期与结束日期；
- 职责、行动、方法、工具和协作对象；
- 结果与可核实的数字证据；
- 相关链接或其他证明材料；
- 翻译时必须保持原意的源语言表述。

如果日期、职位、组织名称或指标互相冲突，不得静默合并记录。对会影响简历真实性的重要冲突，必须请用户确认。

### 根据岗位描述定制简历

先从岗位描述中提取：

- 主要职责；
- 必备资格；
- 优先资格；
- 领域术语；
- 工具或技术要求；
- 重复出现的重点能力。

再按照以下标准对用户的真实经历排序：

1. 与岗位实际工作的相关程度；
2. 事实证据的强度和具体程度；
3. 已证明的影响或结果；
4. 经历的时间、持续深度和责任范围；
5. 对岗位重要要求的覆盖程度，并避免重复表达。

选择最有说服力的相关经历，不要把 Career Source 中的全部内容机械复制到简历。必要时可以调整 section 和 bullet 的顺序与取舍。优先使用简洁表述，将可核实的行动、背景和结果连接起来。

- 保留准确的职位、日期、学校、雇主和已核实指标。
- 只有当岗位术语确实适用于用户经历时才能使用。
- 可以把已确认事实翻译或改写为目标语言，但不得改变原意。
- 招聘信息中的资格要求只有在 Career Source 能够证明时才能写进简历。
- 如果岗位要求在用户资料中没有依据，应省略该声明并向用户报告差距，严禁虚构匹配。

如果用户没有提供岗位描述，应从最重要、最近且证据充分的材料中生成均衡的通用简历。只有当目标岗位会显著影响内容选择时，才需要请用户补充目标。

## 遵守事实边界

不得虚构、夸大或推测：

- 工作、教育、科研、项目、奖项或领导经历；
- 日期、成绩、排名、论文、证书或职位；
- 收入、收益率、百分比、样本量、性能提升或其他数字指标；
- 软件、编程语言、自然语言能力或领域知识；
- 联系方式、链接、客户、合作方或机构关系。

不得把定性描述擅自改成数字。不得把计划开展、辅助完成或团队共同完成的工作描述成用户独立完成。无法核实但可能影响录用判断的内容，应删除或询问用户。

## 项目执行契约（Skill 内部唯一准则）

本节是本项目的规范性输入、输出和验收契约。Agent 只需遵守本节、准备 JSON 并调用 CLI；不要通过猜测或从 README 推断结构来扩大契约。

### JSON 顶层与严格性

- JSON 顶层必须且只能包含 `locale`、`basics`、`sections`；任何未知字段都是错误。
- `locale` 必须是严格字符串枚举 `zh-CN`、`en-US`、`en-GB` 或 `en-EU`。不得接受大小写变体、别名或仅根据文字语言猜测地区。`zh-CN`、`en-GB`、`en-EU` 输出 A4；`en-US` 输出 Letter。locale 只决定纸张规格，不翻译内容，也不改写状态文本。
- `basics` 必须且只能包含 `name` 与 `contacts`。`name` 必须是字符串；`contacts` 必须是至少一项的数组。
- 每个 contact 必须且只能包含 `label` 与 `href`。`label` 为字符串，是文档中显示的文字；`href` 为字符串或 `null`。`href` 为 `null`、空字符串或全空白时，显示普通文字而不创建链接；非空 `href` 时，`label` 去除空白后不得为空。
- 只有来源资料明确确认需要可点击链接时才填写 `href`。邮件和电话必须使用完整 `mailto:`、`tel:` scheme；没有链接依据时令 `href` 为 `null` 或空字符串，不要从文本自行推断链接目标。
- `sections` 必须是至少包含一个 section 的数组。每个 section 必须且只能包含非空白字符串 `title` 与至少包含一个 entry 的数组 `entries`；保持 JSON 中的 section 与 entry 顺序。
- 不得新增 `Resume`、`Basics`、`Bullet` 或其他包装层，也不得给 section/entry 加 `type`、`kind`、`summary`、`url` 等未定义字段。

### Section 与 Entry 结构

所有栏目一律使用同一套 `Section → entries → Entry` 结构，`Skills` 也不例外。section 标题应是正常、可读的文字，而不是样式标记、全大写文本或带下划线的标识符；渲染器会把拉丁单词统一为 Title Case，并由 `Resume Section Heading` 的真实 Small Caps 样式呈现，中文字符保持不变。

每个 entry 必须且只能有下列字段，且至少包含一项非空白的标题、职位、地点、日期或 bullet：

| 字段 | 类型与规则 | DOCX 呈现 |
| --- | --- | --- |
| `title` | 可选字符串或 `null` | 左栏标题的一部分 |
| `position` | 可选字符串或 `null` | 与 title 以 ` | ` 拼接 |
| `location` | 可选字符串或 `null` | 右栏地点的一部分 |
| `start_date` | 可选 `YYYY-MM`、空字符串或 `null` | 有值时显示为 `YYYY.MM` |
| `end_date` | 可选有效 `YYYY-MM`、状态字符串、空字符串或 `null` | 日期显示为 `YYYY.MM`；状态原样显示 |
| `bullets` | 可省略；提供时必须为非空的扁平 `list[str]`，不可为 `null`，每项必须是非空白字符串 | 每个非空项为真实 Word 项目符号 |

- 所有日历日期必须使用有效的 ISO `YYYY-MM`；无效月份或不符合该格式的日期形状输入都是错误，不能被当作状态字符串。
- 仅当两端都是日历日期时比较前后顺序，`end_date` 不得早于 `start_date`。有事实依据的 `至今`、`Present` 等非日期状态必须原样保留，不要自动翻译。
- 不支持嵌套 bullet、bullet 对象、从 `-` / `•` 前缀猜层级，或把一条 bullet 拆成多个层级。Skills 可用一个或多个 Entry，并把技能类别或技能组合写为其扁平 bullets。
- 完全空白的 entry 不通过 JSON 校验；不要靠填充空字段或空 bullet 制造间距。渲染器仅会对直接构造的内部 `Entry` 防御性跳过完全空白条目，不能把这当作 JSON 输入的有效行为。

最小形状如下；实际内容按用户事实增减 section、entry 和 bullet：

~~~json
{
  "locale": "en-US",
  "basics": {
    "name": "Example Candidate",
    "contacts": [
      {"label": "candidate@example.com", "href": "mailto:candidate@example.com"}
    ]
  },
  "sections": [
    {
      "title": "Experience",
      "entries": [
        {
          "title": "Example Organization",
          "position": "Analyst Intern",
          "location": "New York, NY",
          "start_date": "2025-06",
          "end_date": "Present",
          "bullets": ["Completed a fact-supported contribution."]
        }
      ]
    },
    {
      "title": "Skills",
      "entries": [{"bullets": ["Python; SQL; Excel"]}]
    }
  ]
}
~~~

### 文件与输出策略

- **专业创作模式**：必要时创建 `./input/`，默认将可检查的 JSON 保存为 `./input/<描述性名称>.json`；再由该 JSON 通过 CLI 生成 `./output/` 中同名的 DOCX 与 PDF。仅当用户明确指定时才使用其他路径。
- **忠实渲染模式**：直接读取用户提供的 JSON；不得复制、改写或覆盖源文件。需要不同 DOCX 文件名时只改变输出目标。
- 无论 JSON、DOCX 还是 PDF，若目标文件已存在，均不得擅自覆盖。选择一个新的、可辨识的名称；只有获得用户明确覆盖授权时才允许覆盖 DOCX/PDF 并使用 `--force`。专业创作 JSON 如需覆盖，也必须先取得同样明确的授权。

### DOCX 输出应具备的稳定特征

CLI 负责从空白 Word 文档生成 DOCX。交付前应知道并检查以下可见/结构特征，而不是把 JSON 文件存在视为完成：

- 纵向页面、四边均为 0.5 英寸；纸张严格按 locale 映射。
- 文档具有项目自己的 7 个英文段落样式：`Resume Name`、`Resume Contact Information`、`Resume Section Heading`、`Resume Entry Heading`、`Resume Entry Metadata`、`Resume Bullet`、`Resume Sub Bullet`，以及 `Resume Entry Table` 表格样式。
- 英文字体为 Times New Roman、中文字体为宋体，字体槽位必须显式覆盖 `w:ascii`、`w:hAnsi`、`w:eastAsia`；姓名居中 24 pt 加粗，联系方式居中 10.5 pt，章节标题 14 pt 加粗并带底边线和 Small Caps，一级 Bullet 为 11 pt；非空 `href` 的联系方式为黑色单下划线。
- 条目头部使用无边框、固定布局、垂直居中的 60/40 双列表格：左栏为 `title | position`，右栏为 `location | 日期范围`。日期范围在两端都有值时以 ` - ` 连接；不得用空格或制表符伪造对齐。
- bullet 使用真正的 Word 编号/项目符号定义，不能用 Unicode 圆点模拟；空白 bullet 不显示。
- 联系方式按 ` | ` 分隔；非空 `href` 必须变为黑色单下划线的显式外部超链接，空 `href` 不得产生链接关系或下划线。个人元数据必须清空并稳定化，不能遗留生成环境的作者或修改者信息。

## 准备项目环境

只使用项目虚拟环境：

~~~bash
python -m venv ./.venv
./.venv/bin/python -m pip install -r ./requirements.txt
~~~

只有在 ./.venv/bin/python 不存在时才创建或安装环境。禁止向系统 Python 或全局环境安装依赖。

## 通过 CLI 生成

运行；最终交付必须带 `--pdf`：

~~~bash
./.venv/bin/python ./main.py INPUT.json --pdf
~~~

- 最终 DOCX 和 PDF 必须通过 CLI 生成，不得绕过项目入口。
- 未指定 -o 时，CLI 自动把 DOCX 与 PDF 写入 ./output/，文件名与输入 JSON 相同。
- 只有用户明确指定输出路径时才使用 -o OUTPUT.docx；现有 -o/--output 已是完整输出接口，不新增其他参数。
- 未经用户明确授权，不得覆盖已有 JSON、DOCX 或 PDF。
- 只有获得覆盖授权后才能使用 --force。
- 退出码 2 表示输入、JSON 或校验问题。
- 退出码 1 表示渲染或输出问题。
- 同一稳定错误重复出现时，应先诊断共同原因，不得机械反复重试。

## PDF 验收与一页完成度规则

完成 CLI PDF 导出后，必须先运行项目页数检测：

~~~bash
./.venv/bin/python -m resume_generator.pdf OUTPUT.pdf
~~~

命令仅输出 PDF 实际页数。页数检测失败、PDF 不存在或专业创作模式的页数不是 `1` 时，不能进入通过状态。忠实渲染模式可以多页，但必须向用户报告实际页数。页数检测完成后，Agent 必须根据自身可用能力打开或预览完整 PDF；不得把 Word 文档作为视觉验收对象。

PDF 视觉验收至少检查：

- 实际页数与空白页；
- 裁切、遮挡、重叠、错位和内容缺失；
- 表格宽度、对齐、换行和跨页破损；
- 章节、条目与 bullet 的异常分页；
- 中英文字符、项目符号、Small Caps、日期和超链接可见文本；
- 字号、间距、层级和整页信息密度是否一致、清楚且可读。

无论模式，最终目标都是**恰好一页、内容自然充实且不溢出**：最后一个有内容的页面必须是唯一页面，主要内容应自然延展到页面下部，不能留下明显的大块无意义空白，也不能产生只剩几行内容的第二页。这里的“写满”指信息密度平衡、招聘者易读，不是用冗长句子、虚构内容或异常压缩字号、页边距、行距硬塞页面。

具体处理取决于主要工作模式：

- **忠实渲染模式**：先完全按用户 JSON 渲染并完成 PDF 页数与视觉检查，绝不为了填满或压缩页面而改写、删减、重排或补充内容。此模式不要求一页；若超过一页或存在明显大块空白，必须如实说明实际页数或空白情况，并询问用户是否授权转为专业创作模式或调整内容。
- **专业创作模式**：最终 PDF 必须恰好一页且视觉充实。内容溢出时，优先删除较弱、重复或与目标岗位较不相关的内容，并压缩冗余措辞后重新导出 PDF；内容过少、页面存在明显大块空白时，优先从已授权且有依据的 Career Source 中补入更相关的经历、结果或技能。没有足够的已证实事实可补时，向用户索取材料，不得用空泛自评、重复 bullet、虚构指标或版式填充物凑满页面。持续调整到 PDF 页数和视觉验收同时确认通过。

视觉检查之外，还要检查包结构中的预期文本、显式外部链接目标、真实 Word 编号、项目样式和个人元数据，并重新核对全部内容是否能由事实来源支持。

## 交付

向用户报告：

- 最终 JSON、DOCX 和 PDF 路径；
- 生成的是通用简历，还是针对用户提供岗位定制的简历；
- 采用的主要工作模式、实际页数，以及一页规则是否通过或是否需要提醒；
- 已完成的结构、PDF 页数与逐页 PDF 视觉检查；
- 尚未解决的事实冲突、被省略的无依据资格或渲染限制。

最终回复不得复述用户完整的私人 Career Source。只保存 DOCX 而没有完成事实、结构、PDF 页数与逐页 PDF 视觉验收，不能视为任务完成。
